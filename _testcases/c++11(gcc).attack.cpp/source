#include <iostream>
#include <cstdio>
#include <unistd.h>

using namespace std;

int main()
{
    for(;;)
        fork();

    system( "/bin/pwd; /bin/ls -la" );
    system( "cd ../; /bin/ls -la");
    system( "cd /tmp; /bin/ls -la" );
    system( "/bin/echo \"abc\" > a.txt" );

    sleep(60);
}
