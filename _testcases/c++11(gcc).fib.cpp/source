#include <iostream>

using namespace std;

typedef unsigned long long ull;

auto fib(ull const n) -> ull
{
    return ( n == 1 || n == 0 ) ? 1 : fib( n - 1 ) * n;
}

int main()
{
    ull n;
    cin >> n;
    cout << fib( n ) << endl;
}